import { UserResolvers } from "../types/types";

export const User: UserResolvers = {
    // id: (root, args, ctx) => {
    //     return root.id
    // },
    // email: (root, args, ctx) => {},
    // username: (root, args, ctx) => {},
    // createdAt: (root, args, ctx) => {},
    // firstName: (root, args, ctx) => {},
    // lastName: (root, args, ctx) => {},
    // profilePicture: (root, args, ctx) => {}
    // reviews: (root, args, ctx) => {}
}